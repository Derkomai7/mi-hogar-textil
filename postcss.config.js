export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Optional, usually handled by lightningcss/postcss inside tw4 but checking docs, keeping it is standard for compat
    },
}
